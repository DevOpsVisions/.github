name: DevOps Visions Sample Template

on:
  push:
    branches: [ $default-branch ]

jobs:
  greeting:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo "Hello from DevOps Visions"
